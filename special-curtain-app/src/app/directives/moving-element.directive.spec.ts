import { MovingElementDirective } from './moving-element.directive';

describe('MovingElementDirective', () => {
  it('should create an instance', () => {
    const directive = new MovingElementDirective();
    expect(directive).toBeTruthy();
  });
});
